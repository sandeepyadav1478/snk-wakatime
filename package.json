{
  "name": "snk-wakatime",
  "description": "Generates a snake game from WakaTime coding activity or GitHub contributions",
  "version": "1.0.0",
  "private": true,
  "repository": "github:sandeepyadav1478/snk-wakatime",
  "devDependencies": {
    "@types/bun": "1.3.4",
    "prettier": "3.6.2",
    "typescript": "5.9.3"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "type": "tsc",
    "lint": "prettier -c '**/*.{ts,js,json,md,yml,yaml}' '!packages/*/dist/**' '!svg-only/dist/**'",
    "dev:demo": "( cd packages/demo ; npm run dev )",
    "build:demo": "( cd packages/demo ; npm run build )",
    "build:action": "( cd packages/action ; npm run build )"
  }
}
